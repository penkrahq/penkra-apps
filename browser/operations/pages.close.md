# Closing a Browser page

Close one exact `pageId` in the explicitly targeted Browser tab. Closing a hosted page does not
close Penkra itself, uninstall Browser, or disable the App.

If the page ID is stale or unknown, read current Browser state and resolve the intended page again.
Do not close another page based on position or a guessed replacement ID.
