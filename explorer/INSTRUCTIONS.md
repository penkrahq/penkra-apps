# Explorer operations

Use `explorer resources open` when a user asks to inspect an authorized file or directory in the
visual Explorer App. Pass only a handle minted by Penkra; do not infer or fabricate handle IDs.
Explorer can edit supported text files and preview common text, image, and PDF formats. Use Penkra's
core file-opening operation when the resource is a path or URL rather than an existing App handle.
