# Installing an App

Install only the exact registry version the user selected into the current Space. Read that listing
first and pass the complete explicit grant set for its declared permissions. Never infer a Space ID;
the invocation supplies the current Space.

If a required grant is missing, stop for the user's decision rather than copying or inventing a
grant. Verify the returned installation snapshot before reporting the App as installed or usable.
