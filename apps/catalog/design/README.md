# Design

`catalog.pen` is the authoritative design source for the Apps App.

The standard App Bar primitive remains authoritative in `penkra/penkra.pen`. This
directory owns the Apps-specific configuration and content states; the SDK binding is
deferred.
