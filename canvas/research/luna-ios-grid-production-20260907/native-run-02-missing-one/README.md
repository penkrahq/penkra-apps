# Missing-one capture evidence

This directory retains the single approved execution for `grid-c100-180-r60-100-normal` on iPhone `A3D92728-7F7B-44D1-BE51-155B891905D9` at Large.

The installed executable matched the run02 binary SHA-256 and no install or build was performed. The launch command returned exit code `0` with a PID. The runner then produced no receipt query, screenshot, crop, or comparator result. The case is therefore unmeasured, not a visual mismatch and not evidenced as a launch timeout or signal failure. The literal `undefined` failure log and the complete command timeline are preserved; `post-run-audit.json` records the distinction.

The iPhone was initially Shutdown, observed at Large after boot, restored to Large, and shut down. The final device state is Shutdown. Previous run02 and missing-five evidence remain untouched.
