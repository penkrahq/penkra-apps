# Export verification, 2026-09-05

Evidence from the continuation of Decks, after reading every durable conversational message through the final pagination cursor. This is a measurement record, not a replacement plan or authorization for undisclosed architecture. The user-owned plan, instructions, operations prose, and skills were not edited in this continuation.

## Interrupted-turn recovery: rotated raster compositing

The pre-rotation full suite completed with 414/414 passed, zero failed/cancelled/skipped, in 97.273 seconds (`/tmp/canvas-paint-matrix-full-suite.log`). This result predates the rotation changes and is not verification of them.

After Penkra stopped, the retained raster-compositing run was checked before repeating it. It had failed, with exit 1, at SVG 1x. Its reference image was itself clipped: visual bounds treated the graph's transformed origin as an unrotated layout corner, and the opacity layer used world-space bounds in a transformed local canvas. The screenshot path also added a selected node's own rotation displacement twice. Retained failed images and IR are in `research/raster-compositing-20260905/` and `research/raster-compositing-rotation-fixed-20260905/`. Corrections are under active pixel verification; neither failed capture is passing evidence.

After correcting the transformed-origin handling, parent screenshot transform, opacity layer clip and baked raster wrappers, all 24 comparisons passed (four rotation/blend cases, three formats, two scales). The focused run passed 70/70 and the complete suite passed 418/418, zero failed/cancelled/skipped, in 96.739 seconds (`/tmp/canvas-rotation-focused-2.log`, `/tmp/canvas-rotation-full-suite.log`). A further fork geometry check caught stroke geometry receiving overflow twice in the new helper; that was corrected. Four older geometry expectations still assumed radius-sized blur support, inconsistent with the previously measured three-sigma support; those expectations now specify the actual kernel calculation. The fork geometry/world-transform suite passes 86/86 (`/tmp/canvas-rotation-fork-tests-3.log`). The final engine rebuild and post-rebuild rerun remain required before treating the final source state as verified.

The rebuilt rotation state subsequently passed 418/418 in 86.184 seconds (`/tmp/canvas-rotation-final-full-suite.log`); the added ancestor rotation/reflection regression passed in the separate 59/59 engine run. Pixel comparisons and the clipped-reference negative control also passed. However, visual inspection of retained `research/raster-compositing-verified-20260905/` revealed a shared reference/output defect: node-level `multiply` rendered as ordinary blue, whereas paint-level `multiply` rendered the correct dark product. The graph importer had never assigned node blend mode. That capture directory therefore proves rotation/reference agreement, **not correct node blending**. The importer now retains explicit node blend modes without changing the default pass-through behavior. The comparison additionally asserts the independently calculated multiply color `[49,65,58,255]` inside the overlap; this correction is awaiting its own rebuilt verification.

The node-blend rebuild produced engine SHA-256 `0a484a36421cb516466df4b58a42537b580d7053f0ebd994af787e57b0767b5d`. Its focused run passed 60/60, including all 24 pixel comparisons, the independent multiply-color assertions and the clipped-reference negative control (`/tmp/canvas-node-blend-fidelity.log`, 12.121 seconds). Final captures are in `research/raster-compositing-blend-fixed-20260905/`; the corrected HTML node-blend image was visually inspected and now matches paint-level multiply. The complete suite for this engine is running in `/tmp/canvas-node-blend-full-suite.log`.

That complete run finished: **419/419 passed**, zero failed/cancelled/skipped, in 94.468 seconds, exit 0. This is the final rotation/node-blend source verification, not completion of the remaining mobile or PDF/X-4 scope. The next mobile size matrix is 24, 28.25, 32, 40 and 48 pixels, generated into isolated fixture hosts; no capability promotion is based on generating or compiling it alone.

### Five-size mobile text matrix

The isolated hosts in `/var/folders/vb/g066rwxj3mj405k01t_vjkf80000gn/T/canvas-font-delivery-gLMIhv` compiled and ran. Android passes at 420 dpi (1,576,930 interior pixels, glyph counts 1831/4168/5031/10066/16380) and 320 dpi (904,895 pixels, glyph counts 448/1291/1864/4521/7729). Both captures were visually inspected. Only Compose `properties.fontSize` is promoted; `properties.text.run.fontSize` remains gated because differently sized spans still use an integer-rounded Android span path. There are now 76 unresolved mobile rows.

iOS does **not** pass this matrix. The native probe retained as `research/mobile-qa/ios-font-size-metrics.json` reports a requested 28.25-point UIFont unchanged at 28.25, while UIFontMetrics.scaledFont returns 28 at `.large`. scaledValue returns 28⅓ on the 3x device. An isolated SwiftUI `@ScaledMetric` experiment passes the iPhone but not the iPad. A second isolated experiment uses exact fixed sizing at the `.large` reference setting and preserves the existing relative-size path at other Dynamic Type settings. Its fractional row passes both devices, but the iPad still has six mismatched pixels in the 24-point row; rows 28.25/32/40/48 have zero interior mismatches. Making the 24-point row fixed-size did not remove those six pixels. No workaround has been put into the canonical SwiftUI emitter, and no Swift size row is promoted. Original failing captures remain negative evidence in `compatibility/mobile-font-size-fidelity.test.mjs`.

This distinction follows the actual Apple APIs: [custom fixedSize does not scale with Dynamic Type](https://developer.apple.com/documentation/swiftui/font/custom(_:fixedsize:)), whereas [custom size relativeTo does](https://developer.apple.com/documentation/swiftui/font/custom(_:size:relativeto:)). The rounding result above is a measurement, not a claim from those documents. Comparison thresholds remain two channel steps outside the existing two-pixel boundary exclusion. The iPhone's original accessibility-extra-extra-large setting and Android's original 420 dpi were restored after captures.

The updated full suite passes **422/422**, zero failed/cancelled/skipped, in 94.533 seconds (`/tmp/canvas-font-sizes-full-suite.log`). Development build passes (`/tmp/canvas-font-sizes-development-build.log`); production exits 1 with `CANVAS_CAPABILITY_INCOMPLETE`, 39 Swift and 37 Kotlin unresolved rows (`/tmp/canvas-font-sizes-production-gate.log`). The post-format fork geometry/world-transform suite still passes 86/86. No release or protected-document edit was performed.

### Mixed-size Android spans: rounding repaired, baseline fidelity still gated

The five mixed-run rows use a 24-point `Aa ` prefix followed by 24/28.25/32/40/48-point text. The original sp-span host compiled in 53 seconds and matched at 420 dpi, but failed at 320 dpi with per-row mismatch counts `[0,9,0,13,0]`. [Android's implementation](https://android.googlesource.com/platform/frameworks/support/+/e3e6db773dbc2ee4f4b6c2bda8b0b6e3a4db39a5/compose/ui/ui-text/src/androidMain/kotlin/androidx/compose/ui/text/platform/extensions/SpannableExtensions.android.kt) rounds sp spans into AbsoluteSizeSpan integers, whereas em spans use RelativeSizeSpan.

The canonical Compose emitter now derives an em ratio from both authored sizes converted through the current density/font scale, preserving fractional sizing and Android's nonlinear scaling conversion. A zero-size first run selects a nonzero run as its denominator. Generated Kotlin compiled successfully (48 seconds, four executed Gradle tasks). The relative-span capture matches all five rows at 420 dpi (1,593,134 interior pixels), but still fails at 320 dpi with counts `[0,4,0,13,0]`. Both relative captures were visually inspected. Thus the rounding component is reduced, **not all mixed-run fidelity repaired**, and `properties.text.run.fontSize` stays unverified. The thresholds are unchanged; retained tests assert the 320-dpi failures rather than calling them passing visual evidence. Focused exporter/retained tests pass 30/30.

Canvas text measurement reports logical first-line baselines 23, 27, 31, 39 and 47 for these sizes, for both uniform and mixed runs. The remaining mismatches include the smaller prefix, not only the resized run. This is evidence for further baseline/glyph-position investigation, not proof of its cause. Full suite and development build for this relative-span change are running in `/tmp/canvas-mixed-font-sizes-full-suite.log` and `/tmp/canvas-mixed-font-sizes-development-build.log`.

Both completed successfully: full suite **427/427**, zero failed/cancelled/skipped, 91.795 seconds, exit 0; development build exit 0. The production capability count remains 76 unresolved rows. These successes do not close the documented iOS/mixed-run pixel discrepancies or the broader unfinished scope.

## Approved directory convention and read-only backend deployment

### Review findings F, A–E

The user's reported `npm test` result (385 tests, 384 passed, one Swift timeout/cancellation, exit 0) is an incomplete gate, not a passing suite. Historical clean-pass wording must not be applied to that run. `npm test` now uses the public Node test-runner stream and independently requires a successful final summary with no failed or cancelled tests. Subprocess regressions verify exit 0 for success and exit 1 for assertion failure, timeout and explicit cancellation. Missing final summaries also fail closed.

The first complete strict-runner run passed 390/390, with no failures, cancellations or skips, and exit 0 (`/tmp/canvas-strict-suite.log`, 118.6 seconds). Swift tests took 25.100 seconds, the subsequent bundled-font build 1.404 seconds, and Compose assembly 83.180 seconds. The Swift package has no external package dependencies; its fresh temporary copy compiled with the installed toolchain. Earlier isolated/serial runs also took about 21–30 seconds. Missing tools and cold external dependency resolution are therefore not the cause demonstrated here. Resource contention is a hypothesis for the earlier 240-second timeouts, not a proven root cause; this four-way run did not reproduce them. Each compiler phase now reports elapsed time and prints accumulated compiler output on abort/error.

All five vector rows in all six emission profiles were first changed to `status: unverified` with no native verdict. Existing structural and partial visual evidence was retained without treating it as complete fidelity approval. Candidate writer tests used the explicit verification-only IR path, while normal vector export/extraction was tested to reject unresolved capability rows. Subsequent measured promotions are recorded below. The composition function rejects conflicting row verdicts in either order; shadowed vector entries and additional HTML/SVG text, stroke and mobile spacing conflicts were removed without changing their previously effective non-vector verdicts. Focused capability, IR, exporter and legacy migration tests passed 44/44.

The production build was run and exited 1 with `CANVAS_CAPABILITY_INCOMPLETE`: PPTX 5, HTML 5, Swift 51 and Kotlin 50 unresolved rows (111 deliverable rows). Private PDF/SVG emission coverage each has five unresolved vector rows in addition. These private extraction profiles are not deliverable capability tables.

`migrateM2AssignModule` and `migrateM7AssignRoles` are legacy migration stages, called by the migration pipeline and legacy tests. That pipeline converts `print` to `generic`, removes `page` roles, then validates canonical content. The passing legacy-print regression preserves physical dimensions, artwork, bleed and advisory guides while leaving the source untouched. These intermediate legacy values do not reintroduce canonical print/page support.

The seven previously untracked operation documents were committed unchanged in `0accad6` (`Track Canvas operation documentation`). The pre-existing edit to `documents.execute.md` was not included. Import sequence pins and cross-document reference validation remain research-only under the renewed user instruction; no import implementation was changed for these findings.

### Measured SVG, HTML and PDF vector matrix

After first marking all vector rows unverified, `compatibility/vector-fidelity.test.mjs` added actual Chromium/Poppler rendering comparisons against Canvas for 20 cases: even-odd and nonzero variants of square/curved holes, self crossings, polygons, overlapping contours, quadratic/smooth curves, arcs, relative commands, offset viewBoxes and open filled contours. All are nonuniformly scaled. At 1x, each format matches 538,716 uniform interior pixels; at 2x, each matches 2,265,890. The comparison permits two channel steps and excludes only a two-physical-pixel antialiasing boundary. A blank-render negative control is rejected. Structural checks prohibit image substitution for SVG/HTML, with separate native PDF object checks retained.

The first harness launched a fresh command-line browser per capture and timed out at 120 seconds. The strict runner reported one cancellation and exited 1; late-produced images in `vector-fidelity-20260905` are not a passing test. The corrected harness reuses one browser, waits for navigation and capture through CDP, and closes it explicitly. Its test and the existing PDF paint-order/alpha test passed 2/2 in 8.2 seconds (`/tmp/canvas-vector-fidelity-fixed.log`). The successful artifacts and measurements are retained in `vector-fidelity-fixed-20260905`; SVG/HTML/PDF images were visually inspected at both scales.

Only then were the five SVG, HTML and PDF vector rows promoted to native with this measured evidence. At that point PPTX/Swift/Kotlin vector rows remained unverified: 106 unresolved deliverable rows (PPTX 5, Swift 51, Kotlin 50). Other paint, stroke, effect, layout and semantic properties remain independently gated. This measurement does not establish universal exporter fidelity.

### Twenty-case native mobile and PowerPoint matrix

`mobile-vector-fixture.mjs` shares the same 20 cases, rearranged to fit the mobile frame. `scripts/verify-mobile-vectors.mjs` uses an explicit orange registration square to account for the native window's placement, then compares every uniform interior in the declared fixture region. iPhone 3x passed 2,079,474 pixels; iPad 2x passed 908,950; Android 420 dpi (2.625x) passed 1,577,758; Android 320 dpi (2x) passed 908,950. Tolerances are two physical boundary pixels and two color-channel steps. All four screenshots were visually inspected; Android density was restored to its original physical 420 dpi. The reference initially hit the screenshot helper's 2048-pixel cap, producing a rejected measurement. The verifier now requests a 4096 limit, fails on downscaling and asserts exact reference dimensions. Those failed reference measurements were not attributed to the exporters.

The expanded native PowerPoint matrix revealed a real nonzero-fill defect: same-winding nested contours and overlaps incorrectly produced holes. Its retained pre-fix screenshot is `powerpoint-vector-matrix/powerpoint-native-matrix.jpeg`. DrawingML fill lowering now simplifies with the authored fill rule for both nonzero and even-odd, creating boundaries with identical coverage under either destination winding interpretation. Original contours remain in a separate non-filled stroke path. Unit tests verify both source rules under both destination rules, including intersecting/curved/duplicate/open contours.

The corrected 20-custom-geometry PPTX was opened in Microsoft PowerPoint and visually inspected. `compatibility/powerpoint-render-evidence.test.mjs` checks the retained native screenshot against Canvas: 390,274 interior coverage comparisons, zero mismatches. The pre-fix screenshot is a negative control with 9,156 mismatches. The measured slide rectangles are explicit fixture data; a two-pixel boundary tolerance excludes resampling/JPEG edge differences. This is coverage verification, not a color-management or interactive-editing claim. Artifacts are in `powerpoint-vector-matrix-fixed`. The focused vector/writer suite passed 29/29.

Only after these checks were the remaining PPTX/Swift/Kotlin vector rows promoted. All six formats now have measured rendering evidence for the five geometry rows. The broader mobile table still contains 46 Swift and 45 Kotlin unresolved rows; production remains gated. The full suite before the final PowerPoint repair passed 393/393, no failures/cancellations/skips, in 149.9 seconds (`/tmp/canvas-vector-matrix-final-suite.log`). An intervening browser-fixture cleanup race failed a prior full run; cleanup now waits for the exact child close event before deleting its profile, and the focused rerun passed.

### Strict suite and bounded PDF validator follow-up

The full suite after the PowerPoint repair and instance-placement regression passed **395/395**, with zero failures, cancellations or skips, exit 0, in 158.1 seconds (`/tmp/canvas-native-validator-full-suite.log`). Swift compilation took 50.935 seconds including the bundled-font build. This run predates the subsequent Canvas reference-root paint repair.

An earlier run stalled in an unbounded `docker image inspect`, not Swift. The exact test-owned processes were stopped; that interrupted run is not a pass. PDF validator discovery now has a 15-second deadline, validation has a 90-second deadline, and the tests have a 120-second deadline. A missing validator fails rather than skips. The unresponsive Docker daemon produced the expected two failures in the bounded probe (`/tmp/canvas-bounded-pdf-conformance.log`); no unrelated containers or daemon were restarted.

The official native veraPDF 1.30.2 distribution runs on the installed Java toolchain and removes Docker from the normal local validation path. `scripts/install-verapdf.mjs` verifies the detached publisher signature against fingerprint `13DD102B4DD69354D12DE5A83184863278B17FE7`, installs unattended into a new destination, and checks the exact version. The installer SHA-256 is `6cc6341cb1af644044054b81f00a6590a7918abb18f762243de115258bcad838`. An end-to-end fresh installation passed (`/tmp/canvas-verapdf-installer-recheck-fixed.log`); native PDF/A-3b, PDF/UA-1 and the PDF/X-4 fail-closed guard passed 3/3 in 35.9 seconds. PDF/X-4 compliance is still not claimed. Official sources: [installation documentation](https://docs.verapdf.org/install/), [signed release directory](https://software.verapdf.org/rel/1.30/), [publisher key](https://software.verapdf.org/keys/KEY).

### Native frame, group and reference surfaces

The six-case surface fixture found two defects before passing: the canonical resolver discarded reference-instance placement/opacity, and the Canvas engine inherited root paint only from legacy `reusable` nodes even though ordinary frames can be reference targets. Both were repaired with regressions. No library publication, pinning or cross-document validation policy was changed. The engine regression suite passed 56/56.

Opaque and half-opacity frames, groups and references now match Canvas for 2,067,900 interior pixels on iPhone 3x, 899,440 on iPad 2x, 1,553,990 on Android 420 dpi and 899,440 on Android 320 dpi. All four native screenshots were visually inspected; Android density was restored. `scripts/verify-mobile-vectors.mjs --surfaces` uses the magenta registration square and the same two-channel/two-boundary-pixel tolerances as the vector matrix. Corrected screenshots and references are `mobile-qa/*surfaces-fixed*.png`; previous failed captures remain separate. Only `nodes.frame`, `nodes.group`, `nodes.ref` and `properties.opacity` were promoted in each mobile table. Swift now has 42 unresolved rows and Kotlin 41.

The retained surface checks pass 5/5, including the failed-placement negative control. The full post-surface suite passed **401/401**, zero failures/cancellations/skips, exit 0, in 112.9 seconds (`/tmp/canvas-surfaces-full-suite.log`). The development build passed, and the production build failed with the expected 83 unresolved mobile rows. These results precede the typography batch below.

### Typographic family validation

The expanded native font fixture initially failed before compilation: exact-face validation accepted only OpenType name ID 1 and therefore rejected Inter Medium, SemiBold and ExtraBold as members of Inter. Their font files explicitly declare Inter in name ID 16. Validation now accepts declared typographic-family names as well as the legacy family name, while still rejecting incorrect weight, style and unrelated family bytes. The [OpenType naming specification](https://learn.microsoft.com/en-us/typography/opentype/spec/name) distinguishes the legacy four-style grouping from the full typographic family. Focused font tests pass 3/3, including all three formerly rejected faces and unrelated-family negative controls. This is a font identity repair, not yet a typography fidelity verdict.

The actual five-weight native matrix passes at standard text size on iPhone 3x (2,054,157 checked interior pixels, glyph interiors in every face) and iPad 2x (893,111). Android initially failed: its line widths and baselines differed from Canvas. Removing inherited Material defaults did not change that failure and is not credited as its fix. AndroidX's [absolute font-size spans](https://android.googlesource.com/platform/frameworks/support/+/e3e6db773dbc2ee4f4b6c2bda8b0b6e3a4db39a5/compose/ui/ui-text/src/androidMain/kotlin/androidx/compose/ui/text/platform/extensions/SpannableExtensions.android.kt) round to whole physical pixels. Shared font size now lives on the base TextStyle, avoiding the 73.5→74 pixel rounding at 420 dpi. The remaining advance-width mismatch disappeared only after selecting documented [linear/subpixel text rendering](https://developer.android.com/reference/kotlin/androidx/compose/ui/text/style/TextMotion). This flag does not animate the text. The resulting Android 420 dpi capture passes 1,559,356 interior pixels, including all five glyph faces, with the unchanged two-channel/two-boundary-pixel tolerances. Differently sized spans retain a separate gate.

Screenshots are `mobile-qa/ios-font-matrix-large-3x.png`, `ipad-font-matrix-large-2x.png`, and `android-font-matrix-linear-420dpi.png`; each was visually inspected. Earlier Android failed captures remain separate. The iPhone's original accessibility-extra-extra-large setting was restored. The emulator log recorded a graceful shutdown before one installation; the same AVD was restarted headlessly with 2 cores/2048 MB, physical 420 dpi and font scale 1.0. It booted and the corrected APK compiled, installed and launched. No typography row was promoted before completing the density matrix.

Android 320 dpi also passed 893,111 interior pixels with nonempty glyph interiors in all five faces, and its screenshot was visually inspected. Density was restored to physical 420 dpi. `compatibility/mobile-font-fidelity.test.mjs` retains all four passing native captures and rejects both the original rounded-span output and the intermediate base-size-but-hinted output. Only `properties.fontWeight` and `properties.text.run.weight` were promoted in each mobile table. The unresolved inventory is now Swift 40 and Kotlin 39; this does not establish mixed-size runs, all typography or accessibility-scale layout fidelity.

The font matrix's six retained positive/negative tests pass. The complete suite after the typography repairs passed **408/408**, zero failures/cancellations/skips, exit 0, in 90.4 seconds (`/tmp/canvas-font-matrix-full-suite.log`). Swift took 23.5 seconds. The development build also passed. This result precedes the subsequent paint fixture expansion.

### Solid paint alpha and opacity

The six-case solid-paint fixture compares shorthand/full hex alpha, numeric rgba, independent paint opacity, their multiplication and a fully transparent paint. Its first comparison found a Canvas defect: solid fill/stroke conversion retained color alpha but discarded the paint object's opacity. The owned engine now multiplies them; its 57-test suite passes, including a fill/stroke regression. Rebuilt engine SHA-256: `0c64bc1d2bd53f9d451374dcac510e1ee909463973001bfe275a9b4b33e934f9`.

Actual native captures then pass against the repaired Canvas: iPhone 3x 2,111,340 interior pixels; iPad 2x 928,400; Android 420 dpi 1,601,321; Android 320 dpi 928,400. All four were visually inspected. Android density was restored. Native files are `mobile-qa/*paint-matrix*` and repaired references are `canvas-paint-matrix-fixed-*`; the unrepaired Canvas reference is a retained negative control. `compatibility/mobile-paint-fidelity.test.mjs` checks these cases. Only `properties.fill.solid` was promoted for each mobile target; the parent fill row remains unverified for aggregate paint handling. Current unresolved inventory: Swift 39, Kotlin 38.

### Earlier five-case PowerPoint check

The native PowerPoint UI exposed an old Grant File Access modal for the task-owned `powerpoint-qa` folder. Its directory-search sheet required raising before keyboard dismissal worked. The stale request was cancelled, and the corrected `powerpoint-qa-fixed/native-vectors.pptx` was opened directly through macOS. PowerPoint then displayed all five objects. The actual `powerpoint-qa-fixed/powerpoint-native-vectors.jpeg` screenshot and `canvas-native-vectors.png` reference were visually inspected together: square hole, curved hole, self-crossing bowtie, authored pentagon and even-odd overlapping contours match. The fixture already has five custom geometries and no picture objects.

This closes the previously missing native PowerPoint visual check for those five vector cases. PowerPoint displays an activation-required banner and disables editing, so no interactive edit or native save/export is claimed. No subscription, account activation or payment was introduced, and the source presentation was not modified. Other vector paints/transforms/strokes retain their independent verification scope.

### Independent mobile gaps

Both mobile writers previously collapsed row and column spacing into one value and gave `gap` precedence over its axis overrides. SwiftUI now uses column spacing for HStack, row spacing for VStack, separate grid spacing, and a wrapping layout with shared sizing/placement calculations. Compose uses separate horizontal and vertical arrangements. The focused exporter suite passes 23/23.

The generated `--gaps` fixture built and ran on iPhone 17 Pro (iOS 26.3) and the API 36 Pixel 8 emulator. Inspected `mobile-qa/ios-independent-gaps.png` and `mobile-qa/android-independent-gaps.png` each pass `scripts/verify-mobile-gaps.mjs`: all 14 rectangle positions and sizes match within two physical pixels at densities 3 and 2.625. This covers stacks, wrap and a two-column grid with column gap 30, row gap 25, overridden base gap 7, and root gap 14. Only the three gap property rows are promoted; the grid/layout/alignment rows remain separately gated. The initial Android screenshot preceded the completed app launch and found zero fixture regions; it was rejected and recaptured after the activity reported ready.

The emulator initially could not boot because disk space was insufficient. Only the stopped, task-created Dev 52 installation/profile and regenerable Swift fixture build cache were removed; Dev 1 and user documents were preserved. The emulator then booted successfully. Later device commands were delayed but completed; this is not recorded as an environment block.

The complete serial suite passed **383/383**, without failures, cancellations or skips, in 151.3 seconds (`/tmp/canvas-shaped-text-serial-suite.log`). Swift compiled in 21.1 seconds and Compose assembled in 96.4 seconds. The two earlier parallel runs each cancelled Swift at 240 seconds; the serial pass supports resource contention as the explanation but does not erase those failures. The gap capability tests also pass separately, and `git diff --check` passes.

### Independent rounded corners

Mobile container backgrounds ignored corner radius, while rectangles coerced a four-corner array to NaN. Both now emit normalized round-rectangle geometry with Canvas's TL/TR/BR/BL ordering and zero for omitted array entries. Oversized adjacent radii scale proportionally, matching Skia's round rectangle. Geometry coverage tests compare scalar, independent, omitted, negative and oversized values against Skia, and invalid non-numeric inputs fail explicitly.

The `--corners` fixture built and ran on both devices. Inspected `mobile-qa/ios-independent-corners.png` and `mobile-qa/android-independent-corners.png` each contain eight matching rectangle/container regions. `scripts/verify-mobile-corners.mjs` compares their paint coverage directly against Skia with a two-physical-pixel boundary tolerance; both pass. Focused geometry/exporter tests pass 25/25. The mobile corner-radius rows are promoted; this does not establish clipping, effects or strokes. These changes postdate the 383-test serial pass above.

The development build passed and Dev 1 refreshed to package `03c351437d853567cfccfaec1afcef35ee82819e674719857a4944d49592265e`. Its actual editor opened owned QA document `1dcf677b-1e33-496a-a4f6-04988293347e`, selected `blue-label`, entered inline editing with Return, accepted Select All and replacement text `Blue QA`, exited with Escape, and reported Saved with the inspector showing the replacement. Canvas Undo restored `Blue` and disabled Undo again. These are OS-keyboard/UI checks, not a pass of the separate registered `tabs.press` dispatch path. No non-QA document was edited. The measured mobile inventory is now Swift 48 and Kotlin 47 unverified entries.

The subsequent complete serial suite passed **385/385**, with no failures, cancellations or skips, in 133.8 seconds (`/tmp/canvas-gap-corners-full-suite.log`).

### Nested native accessibility

The `--accessibility` fixture adds direct decorative text, a labelled decorative group with a labelled child, and a labelled visible group with a separately labelled child. The first iPhone run passed the existing basic test but failed the two checks for the visible child's label: SwiftUI replaced it with the enclosing group's label. The retained before tree demonstrates the collapse. Non-root SwiftUI containers now explicitly use [contained child accessibility elements](https://developer.apple.com/documentation/swiftui/accessibilitychildbehavior/contain) before their own description; decorative hiding still takes precedence. Android's existing [empty clearAndSetSemantics](https://developer.android.com/develop/ui/compose/accessibility/merging-clearing) suppresses decorative descendants.

The corrected two-test XCUITest suite passed on iPhone at accessibility XXL and iPad at large. Android's two instrumentation tests passed at font scales 1.0 and 2.0, and the setting was restored to 1.0. Native trees preserve root, heading, shape, group and visible-child descriptions and omit every authored decorative label/content string. Evidence is under `mobile-qa/ios-nested-accessibility-before`, `ios-nested-accessibility-fixed`, `ipad-nested-accessibility-fixed`, and `android-nested-accessibility-font1.json` / `font2.json`. Both Apple screenshots were inspected: decorative artwork remains visible; intentionally fixed-size nested text truncates on the XXL phone, so this does not establish text-layout fidelity. Focused unit tests pass 29/29. Description and decorative rows are promoted; other accessibility rows remain gated. These changes postdate the 385-test pass above.

The centered-label PDF failure below was reproduced and repaired in source using the visible renderer's prepared paragraph layout. PDF extraction now uses measured glyph positions, line breaks, vertical offsets and exact embedded-font glyph IDs for measured text-layout properties, retaining Unicode through ActualText. It checks the SHA-256 of the renderer's loaded font bytes against the embedded program and fails on mismatched fonts or missing glyphs. Author `export: image` and unrelated raster requirements remain effective; this is not a blanket capability promotion or a PDF/X-4 claim.

The new `compatibility/pdf-text-render.test.mjs` checks centered labels, right-aligned wrapped text, mixed weights, accented characters, line height and letter spacing. Poppler text extraction preserves the content; the PDF has no Image objects, font-width inspection passes, and every rendered ink-band edge is within one pixel of Canvas. Both `research/pdf-text-qa/pdf.png` and `canvas.png` were visually inspected. The first fixture used invalid `textGrowth: fixed`; it was corrected to the schema's `fixed-width-height` before the passing measurement. Latest focused checks passed 24/24, including font mismatch/missing-glyph negatives and author-image override behavior. The broad run recorded 379 passes and one Swift compilation timeout at 240 seconds; an isolated retry passed unchanged in 23 seconds. These are reported separately, not as an uninterrupted full-suite pass.

The development build passed and the live Dev 1 Canvas tab updated to package `cb2b5a1269f2aa0aa998e27fa7020cf97dd40173c7a477fbd430ca83c82f71da` during the first rebuild. The existing Dev test thread then hit its selected agent connection's usage limit before producing `/tmp/canvas-dev1-shaped-text-20260905.pdf`; the file was confirmed absent. Therefore the repaired writer's installed-operation result is not yet claimed. No new account, subscription, payment, version or registry publication was introduced.

The original Penkra Dev (slot 1) was exercised through its actual desktop UI in thread `b7eb7a2d-9ce7-40d9-b773-6edf496fb7f1`. After the user explicitly approved setup commands in that thread, the public sideload command returned `updated` for the current Canvas `dist` in Space `penkra-personal`; `documents.extract` help then became available. No version was changed or published. The earlier registry-detail view reported 0.2.40 but did not prove which runtime package was serving the operation surface.

Installed native extraction read the owned QA document `1dcf677b-1e33-496a-a4f6-04988293347e` and created `/tmp/canvas-dev1-native-extract-20260905/blue-frame.png` and `orange-frame.png`. Repeating the exact directory request was refused, and before/after SHA-256 values stayed `e423dbdd27afa868ac237d7cf81f8a0981813125c7f38941e1bcc2ae1ea1dae8` and `575de10939ae97eae9adc944a44e4f7418ad8218c2f5796e6057d2efa6518c21`. No screenshot or shell-writing fallback was used for these artifacts.

The same installed operation produced `/tmp/canvas-dev1-native-extract-20260905.pdf`. Independent Poppler inspection confirmed two 240x160-point pages, and both rendered pages were visually inspected. This is not a preserved-text PDF pass: the operation reported text-layout raster consequences for both labels, `pdffonts` found no referenced fonts, `pdftotext` returned no label text, and `pdfimages` identified one 240x40 label image plus alpha mask per page. The writer still falls short of the agreed native-text extraction behavior for this ordinary centered-label fixture.

The Dev thread created a separate mobile QA document `86ec8d06-da06-4924-ab0a-69886a3103ec`, opened it in tab `3931814f-4166-4b01-b022-a02494a1d7e2`, and used the installed fit-in-view control. Desktop screenshots showed automatic `ios · Phone` and `android · Tablet` captions on unselected frames, and the roleless `Sketch` caption without a role. A selected frame's caption was hidden by the existing selection rendering; this test does not claim selected-caption visibility. Returning through the actual Back to files button showed `Role label check` with module `mobile`; the older module-less QA document showed `Unassigned`. This establishes live module-label loading and unselected role captions in slot 1, using runtime package `d52ebcc070d5315e6e6ae1fef2518372f2645bf6dacbf00c2c447da668779a7d`.

The subsequent full Canvas suite passed 379/379 (`/tmp/canvas-directory-full-suite.log`, 301.38 seconds). The user then explicitly chose ordinary unrestricted Node controllers rather than a managed-job SDK. Host source now launches controllers with empty execArgv while preserving environment filtering and SDK authorization. Seven controller lifecycle/environment tests and the real Electron controller-bundle verification passed. The latter executes a child program, receives a worker message, initializes WASI, and verifies that a missing native binary reaches the loader (`ERR_DLOPEN_FAILED`) rather than being permission-denied; it does not claim arbitrary native add-on compatibility. No job registry, process-tree tracking, or descendant-cleanup guarantee was added. Runtime docs were updated; protected instruction/operation/skill/plan text was not. These host changes await desktop verification and the user's production release.

The user approved deployment and chose a directory destination. A trailing `/` now means a new directory, with one `<nodeId>.<format>` artifact per requested node. Exact file destinations retain their supplied names. All units render before directory creation, and unsafe names, case-folded collisions, missing nodes, and existing destinations fail without publishing the extraction.

The backend release was isolated from four unrelated unpublished commits: branch `codex/canvas-readonly-state-release`, commit `7ad4dd4a1728471ec290d6986bbccdad52d3d8c4`, based on the previously deployed commit. The isolated build and all 214 server tests passed. [Deployment 33986084768](https://github.com/penkrahq/penkra-backend/actions/runs/33986084768) succeeded. Public `/health` and `/ready` returned production status ok, database/storage ok, and the existing payments-not-configured baseline. No schema migration was deployed. The earlier paragraph calling the state endpoint undeployed is superseded by this result; installed module-label UI verification remains unclaimed.

A native exclusive-rename experiment passed local tests but failed in the installed controller: `Access to this API has been restricted. Use --allow-child-process to manage permissions.` This agrees with the public controller policy. The experiment was removed; no process restriction, host permission or native add-on gate was relaxed. Ordinary directory rename can replace an existing empty directory, as documented by [Apple's rename manual](https://github.com/apple-oss-distributions/xnu/blob/main/bsd/man/man2/rename.2), so it is not used as a no-overwrite primitive.

`writeExclusiveBundle` now stages complete files, exclusively reserves the destination with mkdir, and publishes each complete file using a no-replace hard link. This is not whole-directory atomic visibility. A post-reservation I/O failure reports `CANVAS_EXPORT_PARTIAL` and the complete artifacts left behind; it does not recursively remove a destination that another actor may have modified. Focused extraction/writer tests pass 10/10, including eight concurrent writers with exactly one complete winning bundle and rejection of an existing empty directory.

The supported build passed public App testing and was sideloaded successfully. Installed `documents.extract` wrote two PNGs under `/tmp/canvas-controller-directory.3wuwjr/images/`: `0ac578cc-df5b-440f-9a90-27cc5637050e.png` and `qa-rich-text.png`. The frame PNG was visually inspected. Repeating extraction into that directory failed; SHA-256 remained `ec7d388e8926c7293a62e71e1057e71aebede22b173c78d8b73c25c58ec3402d` and `cd40917e626e79e55463ce8ae8d0c1cbe9415b5f846cc6e3da98eb9007951374`. Only the pre-existing owned QA document was read, with no content mutation. A new public document-open attempt again did not return; its orchestration wait was stopped, and this is not attributed to visual tab visibility.

## Implementation follow-up: tokens, style identity, and PDF extraction

The user explicitly authorized objective implementation after the research-only hold. Protected prose remains untouched. The bounded-concurrency suite passed 362/362 after the DEC6/DEC9 and migration corrections. After dotted Canvas references, type-preserving whole-value aliases, rich-text range interpolation, and source-owned imported paragraph styles, the full suite passed 365/365 (`/tmp/canvas-token-style-full-suite.log`, 278.54 seconds). This is not a claim of complete DTCG interchange or library release/publication support.

Single-node roleless PDF extraction now has service tests for 72-DPI fallback, declared physical trim dimensions, real bleed boxes, and fail-closed PDF/X-4. Fold/safe-margin guides emit no marks. Text size now follows the output geometry scale rather than a constant 0.75. Focused extraction tests passed 4/4; the artifact-writer set passed 23/23. These PDF changes postdate the 365-test full run.

`scripts/verify-roleless-pdf.mjs` produced an inspected Poppler render with native even-odd paths, a five-sided polygon, and embedded, extractable Inter text. The first fixture accidentally used the default horizontal layout; its corrected `layout: "none"` fixture is `tmp/pdfs/roleless-extraction-fixed.pdf`. Neither this limited fixture nor successful extraction establishes universal PDF fidelity or PDF/X conformance. Multi-node/directory extraction and removal of the legacy page capability plumbing are still unfinished.

The host keyboard unit/integration tests passed 25/25. Penkra Dev 3 was launched through its numbered launcher and reported running; manual desktop verification has not yet been completed. Atomic single-file writes now publish complete bytes with an exclusive hard link, and bundle path/case/prefix collisions are checked before writes. Directory publication still needs a race-safe no-replace implementation.

The subsequent full roleless-PDF suite passed 367/367 (`/tmp/canvas-roleless-pdf-full-suite.log`, 102.52 seconds). Migration now fails before copy creation if canonical validation cannot preserve the transformed content, instead of substituting an empty frame. Generic documents do not acquire invented roles, and legacy node `export: live` becomes `default` without changing import `pin: live`. The migration test set passed 4/4.

Mobile font delivery now packages used font bytes, exact face selection, native registration/loading helpers, integration instructions, and Inter's OFL license. Missing faces and mislabeled weight/style/family bytes fail explicitly. Generated Swift and Compose font-aware sources compiled in the pinned fixtures (2/2, 105.76 seconds). Separate device hosts were generated by `scripts/generate-font-delivery-fixtures.mjs` at `/var/folders/vb/g066rwxj3mj405k01t_vjkf80000gn/T/canvas-font-delivery-QNzxSu`. Both built and ran as `com.penkra.canvas.qa.fonts`. The iOS app has no UIAppFonts entry; it registers its exported resource files. Android loads exported assets without a network font provider. These hosts and scratch projects are retained for ongoing QA.

Inspected device images: `mobile-qa/ios-bundled-fonts-large.png`, `mobile-qa/ios-bundled-fonts-xxl-settled.png`, and `mobile-qa/android-bundled-fonts-default.png`. All five supplied Inter weights are visibly distinct. The XXL fixture truncates fixed-height text, and the minimal Android host overlaps the status region; these are not broad layout/accessibility passes. `ios-bundled-fonts-xxl.png` caught the launch transition and is not verification evidence. The iPhone's original accessibility-extra-extra-large setting was restored. No mobile capability row was promoted from these limited checks.

Native font references: [Apple custom-font guidance](https://developer.apple.com/documentation/swiftui/applying-custom-fonts-to-text), [Android font guidance](https://developer.android.com/develop/ui/compose/text/fonts?hl=en), and [Inter's distributed license](https://raw.githubusercontent.com/rsms/inter/v4.0/LICENSE.txt). Source bundles also now reject colliding normalized screen/route names and reserved helper names before a Map can silently discard an output. The focused font/migration/service/operation set passed 17/17 before the additional collision regression test.

The font-delivery full suite passed 370/370 (281.55 seconds). Subsequent DEC1/format work removed print/page from canonical modules/roles, SetModule, creation, and deliverable export. A4/Letter are generic physical-frame presets. Legacy print migration preserves artwork and physical/bleed/guide fields while removing the retired module/role. Capability tables are keyed by pptx/html/swift/kotlin; PDF/SVG use separate internal writer-coverage checks, and PAGE_PROFILE_DELTAS was removed. PDF/X remains independently fail-closed. PDF/A and PDF/UA conformance fixtures now use extraction; an empty non-painting frame initially triggered an erroneous alt-text requirement, fixed by not emitting/tagging a node that paints nothing. Poppler and the pinned veraPDF fixtures passed again. Multi-node PDF files retain requested order and independent dimensions; multi-node PNG/SVG file requests fail before writing. Directory destination discrimination remains a pending user question.

The full DEC1/format suite passed 375/375 (`/tmp/canvas-dec1-format-full-suite.log`, 253.06 seconds), before the read-only label API correction below. Imported mode resolution now also applies library defaults when the consumer has no corresponding axis and retains unrelated ambient axes without rejecting a local mode override.

### Read-only module labels: discovered backend side effect

Live list metadata showed lastOpenedAt advancing across the document collection. Backend source confirmed that GET /projects/:id unconditionally records an open. The label reader introduced earlier used that endpoint, so it was unsafe for metadata scanning. No attempt was made to reconstruct or overwrite historical timestamps. Canvas now requests the distinct GET /projects/:id/state endpoint and never falls back to GET /projects/:id; older servers fail safely with unavailable labels. The corrected development package is sideloaded.

The backend change is local and undeployed: getAppProject accepts recordOpened=false, uses the existing authorized shared project lock, and returns snapshot plus updates without changing last_opened_at. The new namespace-relative state route selects that mode. Backend domain tests passed 17/17; Canvas API/label tests passed 21/21, including the no-fallback regression. Shared-backend deployment requires confirmation; manual desktop verification is still outstanding. User-owned architecture, operations, instruction, and skill text remains untouched. The withdrawn print skill file was not edited; only its active manifest contribution was removed with the print module.

## Android observation

The installed `penkra_api36_pixel8` emulator launched the generated Compose fixture. Its reported density was 420 dpi (2.625 pixels per dp). The 88-dp logo initially occupied roughly 88 physical pixels: the exporter supplied logical path coordinates directly to the pixel-based drawing canvas. Scaling the canvas by its actual size divided by the authored geometry fixes that discrepancy.

Evidence: `mobile-qa/android-pixel8-before.png` and `mobile-qa/android-pixel8-density-fixed.png`, both inspected visually. The rebuilt APK assembled and installed successfully. These screenshots also expose unresolved layout defects: the circle is stretched by grid constraints, root padding is absent, and content overlaps the status region. This fixture is not evidence that all mobile properties are faithful.

The inherited blanket mobile measurement claims were withdrawn. There are 53 iOS and 52 Android unverified property entries: the original 103 plus two per-run letter-spacing entries added by the unfinished implementation. Device/toolchain availability is not the blocker. Per-property implementation and verification remain incomplete.

## Native path corrections

SVG commands now normalize through pinned `svgpath` 2.6.0, retaining explicit moves and closes instead of inferring topology from shared network vertices. Malformed or non-finite paths fail explicitly. Relative commands, smooth curves, quadratic curves, and arcs lower to absolute move/line/cubic/close commands.

The inherited PPTX even-odd conversion used endpoint polygons and omitted open paths. The replacement uses Skia PathOps to split intersections while retaining curves, then orients the disjoint contours using interior probes and polynomial signed areas. A test established that Skia's `makeAsWinding()` alone incorrectly filled the overlap of two squares; that shortcut is not used. The filled geometry and original stroked contours are separate DrawingML paths so fill conversion cannot create stroke seams or close authored open strokes.

`src/vector-path.test.mjs` checks open topology, malformed input, normalization, and 2,500 interior samples per case for intersecting rectangles, curved nested contours, a self-intersecting bow tie, cancelling identical contours, and an implicitly closed fill with open authored stroke. These are geometry-coverage tests, not PowerPoint renderer measurements. Artifact tests also check native XML emission. Cross-target visual verification remains separate work.

## Extraction and module checks

Roleless extraction now uses its rebased visual origin when calculating raster outsets, reports raster/ignored-property consequences, and rejects non-positive or non-finite scales. Regression evidence covers a 100 × 80 node with asymmetric shadow outsets, yielding a 140 × 120 extraction and a 280 × 240 raster at 2×.

SVG and PPTX preserve depth-first paint order instead of sorting sibling-local indices across the whole flattened tree. A nested-sibling regression catches the prior SVG reordering.

Script tests verify generic-to-deck/print/web/mobile assignment preserves artwork, rejects a second assignment, and rejects role-bearing input. An actual parsed PDF verifies a declared 841 × 1189 mm page without a paper-name whitelist.

## Build harness

The first complete regression run was 297/298. Swift compilation failed because the test copied a generated `FlowLayout.swift` and then wrote another under `_canvas`, producing duplicate output objects. The fixture test now uses the same flattened helper path as the simulator generator; the isolated Swift compilation passes. The development build passes. Production remains fail-closed on mobile property verification and PDF/X-4 preflight; it is not release-ready.

The final complete regression run passed **305/305**, including Swift compilation, Compose assembly, pinned veraPDF PDF/A-3 and PDF/UA-1 checks, and Chrome web-export tests. Scoped `git diff --check` also passed. These checks do not substitute for the outstanding mobile visual/accessibility matrix or vector-specific target-renderer comparisons.

The separate cited import findings are in `import-prior-art-2026-09-05.md`. No import behavior was implemented from that research.

## Live transport verification, 2026-09-05 07:59 UTC

The installed development package successfully executed `Print(1)` against Atferd (`64b13c04-b4c7-4bf5-9c37-2069044b1d55`) twice: **13,162 ms** and **12,979 ms**, including the operation tool round trip. Both responses returned `changed: false`, `operationId: null`, `sequence: 0`, `prints: [1]`, and empty issues and screenshots. No source document was written or compacted. The historical 30-second transport gate is satisfied by these runs.

The initial live probe returned HTTP 400 for `length=8388608`. The reader now obtains the deployed server's default range size and retrieves the remaining immutable snapshot ranges with at most four concurrent requests. It uses the range response's `totalBytes`: document metadata counts compact JSON, whereas the range endpoint serializes PostgreSQL JSONB text with different whitespace. The bounded-concurrency test deliberately models that discrepancy. Backend errors now identify the method, request path, and status without request bodies or credentials. No backend code or deployment was changed.

The development package passed `penkra app test` and was sideloaded into the current Space without a version bump or registry publication. Its registered operation set includes `documents.extract`, not the deleted `documents.export-image`.

## Root effects and live editor observation

Root-frame raster verdicts now enter the actual compositing scope instead of leaving unsupported root paint or layout in a live container. The generated host is neutral and references the complete rasterized root subtree. A regression checks the root raster and its consequence. Nested output geometry also uses the frame's absolute origin.

That test exposed a renderer-bound crash: ordinary layer blur had been treated as though it contained shadow spread and offsets. The owned scene-graph source now applies spread and offset only to drop shadows. The engine rebuilt successfully with SHA-256 `bfdf1bbeeeb948ba91c59156b81df897700af37e841c1aa7297fe59eb5903fab`.

The existing rich-text QA document opened in tab `6ea43188-c3e8-4c10-a023-e5a9ccc95471`. `tabs.current` returned null and the retained snapshot was only `- document "Canvas"`. This is not a live-editor verification pass.

## PDF/X-4 tool audit

Docker Desktop is operational. The official `callassoftware/pdftoolbox-cli` image was installed, digest `sha256:d4a1b7f9fe700f37bd3a3fbeb3492c1838f0d8073c468f900f007e3bb4c7dd29`. Running `./pdfToolbox --status` with networking disabled reports `No License` and `Activation None`. Running its bundled `sample.kfpx` against `sample.pdf` exits **100**, with `Error 1008: Not activated (no license)`. No client files were mounted or uploaded in either probe.

The [official activation instructions](https://hilfe.callassoftware.com/m/59504/l/665202-installation-and-activation-of-pdftoolbox-server-cli) require a license or trial activation, including a registration request to callas. The [trial page](https://callassoftware.com/pdftoolbox-trial/) describes a free account and 14-day activation. No account was created, no registration was submitted, and no trial was activated on the user's behalf. The missing item is a valid activation, not a simulator, Docker installation, or executable.

PowerPoint is installed and responds to AppleScript with version `16.112.3`; the historical claim that no local PowerPoint runner exists is incorrect. This version response alone does not constitute a rendering or no-font test.

Verification after the transport and root-effect changes: the main regression command passed **307/307** and the exporter subdirectory passed **11/11**, for **318 passing tests across the two runs**, with no skips. The package test script now includes `src/exporters/*.test.mjs`; it previously omitted that subdirectory. Development build and package validation pass. No mobile capability row or PDF/X-4 conformance claim was promoted on the strength of compilation or these unit tests.

## Local PDF/X-4 preflight

The commercial activation finding above is not a project prerequisite. Following the user's instruction, `src/exporters/pdfx-preflight.mjs` now checks serialized PDF objects locally; `node scripts/preflight-pdfx.mjs <file.pdf>` prints a structured report and exits nonzero for invalid or incompletely checked files. No subscription, account, network service or commercial validator is required to run it.

The initial checks cover output-intent cardinality and embedded ICC header/tag ranges, page-box geometry, font-program presence, prohibited stream filters, external streams, PostScript XObjects and actions. Optional features outside the checked Canvas subset are rejected conservatively. The report explicitly retains uncovered semantic requirements and cannot report conformance merely because these structural checks pass. Tests include deliberately malformed serialized objects and a fake profile/XMP fixture that demonstrates this distinction.

Reading [ISO 15930-7:2010, clause 6.4.2.1](https://previewnorm.com/iso/ISO%2015930-7-2010%20PDF.pdf) exposed a concrete exporter prerequisite: the destination ICC profile must have output-device class `prtr`. The shipped `assets/color/sRGB2014.icc` actually has class `mntr`, RGB data and XYZ PCS. The checker rejects it as a PDF/X output profile. Changing that signature alone would not create a valid printing transform; the negative tests expressly avoid treating that as a fix. The existing PDF/A use of this profile is unchanged.

This is evidence for the PDF conformance work in `TODO.md`, not a replacement plan or a completed PDF/X-4 certification claim.

Verification: all **325 tests passed**, with no skips, including seven new preflight tests; the development build passed. The local CLI rejected the existing `output/pdf/canvas-bleed-box-qa.pdf` for missing PDF/X output intent and XMP. Poppler rendered that same file successfully and the page was visually inspected; that rendering is appearance evidence only, not conformance evidence.

## Mobile grid measurement and compile harness, 2026-09-05 09:30 UTC

The later 337-test run was not clean: 336 passed and the Swift fixture timed out at 240 seconds. An isolated existing-package Swift run passed, followed by a clean temporary-package run in 18.6 seconds after harness changes. The harness now excludes `.build`, `.gradle`, and generated `build` directories while copying rather than copying and deleting them, limits Swift compilation to two jobs, forwards test cancellation to its child, and instantiates the freshly exported `GeneratedScreen` as well as the static fixture. The original cache measured 234 MB; avoiding that copy is a concrete improvement, not proof that copying alone caused the earlier timeout.

The native-device fixture generator had become unable to exercise root padding after root raster handling was corrected. Its explicit internal capability-verification path can now test a listed candidate even when the production verdict is raster. This does not change production verdicts or add an author force-native option. A regression verifies ordinary request properties cannot bypass the production raster decision. Candidate screenshots must not be represented as production-native coverage.

On the actual API 36 Pixel 8 emulator, the generated Compose grid imposed its cell's minimum width on a smaller child. `Modifier.wrapContentSize(Alignment.TopStart)` now releases that minimum before applying the authored dimensions. This follows [Compose's documented constraint behavior](https://developer.android.com/reference/kotlin/androidx/compose/foundation/layout/wrapContentSize.modifier).

The authored orange ellipse is 96 × 96 dp. Connected-color measurements from the running APK are:

| Screenshot | Device density | Measured solid-ink bounds | Expected authored dimensions |
| --- | --- | --- | --- |
| `mobile-qa/android-grid-before.png` | 420 dpi | 430 × 252 px | 252 × 252 px |
| `mobile-qa/android-grid-after-420.png` | 420 dpi | 252 × 252 px | 252 × 252 px |
| `mobile-qa/android-grid-after-320.png` | 320 dpi | 192 × 192 px | 192 × 192 px |

Before and after at 420 dpi were also visually inspected. Measurement command: `node scripts/measure-mobile-color-regions.mjs <screenshot>`; it finds connected opaque regions within one RGB channel value of F4A261, excluding antialiased fringe pixels. The separate orange chip is reported separately. The emulator density override was reset to its original physical 420 dpi; font scale remained 1.0.

The modified APK compiled, installed and cold-launched successfully. The scoped IR/exporter suite passed 19/19, including the grid sizing-order and production-verification-boundary regressions. This closes the observed child-stretch defect, not the complete grid/ellipse property matrix. Root padding, arbitrary track behavior, text fidelity and accessibility are not established by these screenshots. No mobile capability row was promoted from this limited result. Active work remains governed by `../TODO.md` and the user's subsequent instructions, not this evidence record.

Final clean-copy mobile compilation run: 2/2 passed, Swift 19.96 seconds and Compose 78.23 seconds, no skips or cancellations. The development build also passed. This scoped run does not replace the last full-suite result.

## Android platform accessibility measurement, 2026-09-05

The initial live `uiautomator dump` exposed the heading's description and the circle label, but omitted the authored root-frame description. Code inspection also found no Compose handling for `decorative`. The root now uses the same semantic modifier path as descendants, and decorative nodes use empty `clearAndSetSemantics` while retaining their visual content. [Android's clearing-semantics behavior](https://developer.android.com/develop/ui/compose/accessibility/merging-clearing) supports suppressing the node/descendant semantics in this case.

`compatibility/mobile-fixtures/compose/app/src/androidTest/java/com/penkra/canvas/fixture/AccessibilityExportTest.java` launches the generated application on the emulator and traverses actual `AccessibilityNodeInfo` objects using UiAutomation. It asserts root and shape descriptions, the native `isHeading` flag, ordinary text exposure, and absence of the decorative watermark text. This is not a source-string-only test and does not infer heading behavior from the older XML dump, which did not include that flag.

The connected instrumentation run passed on API 36. Direct reruns passed at 420 dpi with font scales 1.0 and 1.3 (about 3.3 seconds each). Retained tree evidence:

- `mobile-qa/android-accessibility-420-font1.json`
- `mobile-qa/android-accessibility-420-font1.3.json`

Both trees contain the root description, the heading with `heading: true`, ordinary body text, and the circle description; neither exposes the decorative watermark. `mobile-qa/android-accessibility-visible.png` was visually inspected and confirms the watermark is still painted. The font scale was restored to 1.0. Gradle's first connected run removed the test applications during cleanup; the direct reruns reinstalled only the QA application and its instrumentation APK to retain evidence.

Scoped IR/exporter tests passed 20/20 and the development build passed. The assertions cover these specific native fixture cases, not TalkBack speech/traversal quality, every node type or nested decorative subtree, iOS accessibility, or the full mobile capability inventory. No broad property verdict was promoted from these cases. This evidence belongs to the existing mobile verification work in `../TODO.md`; no newly recorded architecture decision was implemented.

## iOS accessibility runtime checks, 2026-09-05

Added an actual simulator application/XCUITest target under `compatibility/mobile-fixtures/swiftui`, generated from `project.yml` with locally installed XcodeGen 2.46.0. The generated Xcode project is ignored; the small declarative project specification and UI test are retained. No signing account or paid runner was required.

The baseline iPhone test failed: its captured accessibility tree omitted the root frame's description. Applying root semantics fixed that assertion. The next assertion exposed decorative text in XCUITest despite `.accessibilityHidden(true)`. Adding an ignored-children accessibility element did not fix that observation. The retained emission uses `.accessibilityRepresentation { EmptyView() }.accessibilityHidden(true)` for decorative nodes, preserving their drawing while replacing their accessible content. Descriptions no longer take precedence over decorative hiding. Apple documents replacing a view's accessibility representation in [SwiftUI Accessibility: Beyond the basics](https://developer.apple.com/videos/play/wwdc2021/10119/).

Final XCUITest runs passed, with zero skips:

- iPhone 17 Pro, iOS 26.3.1: existing `accessibility-extra-extra-large` content-size setting.
- iPad mini (A17 Pro), iOS 26.3.1: existing `large` content-size setting.

The test checks root description, heading text label, shape description, ordinary body text, and absence of the decorative watermark's text from the queried static-text elements. Retained before/failing and passing trees/screenshots are under `mobile-qa/ios-accessibility-before`, `mobile-qa/ios-accessibility-after`, `mobile-qa/ios-accessibility-passed`, and `mobile-qa/ios-accessibility-ipad`. Failed intermediate result bundles remain under `tmp/` (the initial baseline bundle and derived build directory are in the sibling `../tmp/` created by this QA run).

The passing iPhone screenshot was visually inspected: the decorative watermark is still drawn. It also shows severe enlarged-text truncation, so passing these label assertions does not establish text/layout fidelity. The debug tree retains anonymous geometry elements for decorative artwork; this test does not prove their VoiceOver traversal behavior or iOS header traits. Unlike the Android instrumentation test, XCUITest here asserts the heading's label, not an `isHeading` flag. Device content-size settings were observed, not changed.

The scoped IR/exporter suite passed 21/21 and the development build passed. No capability row was promoted on these limited label checks; broader coverage remains in the owning App TODO and explicit user instructions.

## Native font availability isolation, 2026-09-05

The iOS QA host now has an opt-in `--canvas-font-probe` that queries UIKit's font registry and writes its actual results from inside the simulator application. The original run found no Inter family faces, no UIAppFonts registration, and failed UIFont lookup for Inter, Inter-Regular, Inter-Medium, Inter-SemiBold and Inter-Bold. Retained evidence: `mobile-qa/ios-font-probe.json`.

The QA Xcode project now includes the repository's five existing Inter faces and registers them in the test application's generated Info.plist, following [Apple's custom-font setup](https://developer.apple.com/documentation/swiftui/applying-custom-fonts-to-text). This changes the measurement host only, not the unapproved deliverable bundle contract. The second native probe resolved every requested face, including Inter → Inter-Regular, and listed all five registered faces. Evidence: `mobile-qa/ios-font-loaded.json`.

The actual iPhone XCUITest passed again with fonts present. Its attached screenshot under `mobile-qa/ios-font-loaded/` was visually inspected and still shows truncated text at accessibility-extra-extra-large. Therefore missing fonts were real, but not the sole explanation for truncation. The fixture uses explicit text bounds; a correct growth assessment must distinguish intentionally fixed boxes from auto-growing text rather than relaxing all bounds or disabling Dynamic Type to force a pass.

Code inspection confirms production mobile delivery is still different from the QA host: `src/export-service.mjs` provides font bytes to PDF/PPTX but not either mobile writer. SwiftUI emits a font name; Compose's SpanStyle currently omits `fontFamily` entirely. Android documents explicit custom FontFamily/resource binding in [Work with fonts](https://developer.android.com/develop/ui/compose/text/fonts). These facts prevent promoting mobile font-family fidelity from fixture screenshots. No production font-packaging policy or new architecture decision was implemented by this diagnostic.

## Explicit text growth preservation, 2026-09-05

`textGrowth` was included in capability checking but discarded from the IR, and both native mobile writers always emitted fixed width and height. The IR now retains the authored mode in layout metadata. Mobile text explicitly declaring `fixed-width` retains its width and measures its height intrinsically; `auto` requests intrinsic dimensions. Fixed-width-height and unspecified modes keep their existing fixed sizing. The change does not disable Dynamic Type or change every text node to auto-growth.

`node scripts/generate-mobile-qa-fixtures.mjs --growing-text` marks only the fixture heading/body as fixed-width. The default fixture generation remains unchanged. The current generated QA files were produced using that flag.

The actual iPhone run, with registered Inter and accessibility XXL, passed its accessibility checks. The screenshot in `mobile-qa/ios-text-growth/` was inspected: the full heading and body are drawn over multiple lines instead of ellipses. The native tree reports 184 pt heading height and 111.3 pt body height. This contrasts with the earlier fixed-height fixture; the authored sizing intent intentionally differs between those two cases.

The Compose APK rebuilt, installed and launched at font scale 2.0. `mobile-qa/android-text-growth-font2.png` was inspected: the growing body wraps and retains its full text; the watermark, intentionally left fixed-height, still clips. Android font scale was restored to 1.0. Android still lacks the authored font-family binding, so this is growth evidence in the current native font environment, not Inter fidelity evidence.

The scoped IR/exporter suite passed 22/22 and the development build passed. Tests cover preservation and emission of all three declared modes. Native screenshots here cover fixed-width growth in a vertical container, not the full auto-width/absolute-position/grid matrix; no broad capability row was promoted on that narrower evidence.

## Full regression after mobile fixes, 2026-09-05

`bun run test` completed successfully: 342 tests passed, zero failures, cancellations or skips, in 117.5 seconds. This includes the native compilation fixtures; it does not establish all mobile property fidelity. The retained local run log is `/tmp/canvas-regression-20260905-final.log`.

The capability inventory remains 53 unverified iOS rows, 52 Android rows, and one PDF/X-4 profile entry. Further inspection found that `semanticLayout` omits `alignItems`, `justifyContent` and `layoutPosition`, although capability checking includes them and SwiftUI reads `alignItems`. This is an unresolved emitter/IR gap, not a measured native capability. Production mobile font delivery remains separate from the fixture-only registration above and awaits clarification against the unapproved bundle contract.

## Linear cross-axis alignment and SwiftUI container paint, 2026-09-05

The IR now retains `alignItems`, `justifyContent` and `layoutPosition`. The latter two are retained metadata, not a claim that all their semantics are emitted. Non-wrapping SwiftUI stacks now align their surrounding authored-size frame on the same cross axis. Compose rows/columns emit their corresponding cross-axis alignment arguments. References: [Apple stack layout](https://developer.apple.com/documentation/swiftui/building-layouts-with-stack-views), [Apple frame alignment](https://developer.apple.com/documentation/swiftui/view/frame(width:height:alignment:)), and [Android Compose layout basics](https://developer.android.com/develop/ui/compose/layouts/basics).

`generate-mobile-qa-fixtures.mjs --alignment` produces six 300×90 containers with 60×30 probes: vertical start/center/end, then horizontal start/center/end. Actual iPhone and Android applications were rebuilt, installed and launched. Screenshots were inspected and measured with `measure-mobile-color-regions.mjs`:

- iPhone at 3 px/pt: vertical probe x = 14, 374, 734 px, giving relative offsets 0, 120, 240 pt. Horizontal probe y = 1011, 1413, 1815 px against container origins 1011, 1323, 1635, giving 0, 30, 60 pt. Probe bounds are 180×90 px.
- Android at 420 dpi: vertical x = 0, 315, 630 px, giving 0, 120, 240 dp. Horizontal y = 819, 1171, 1522 px against row origins 819, 1092, 1365, giving 0, approximately 30, approximately 60 dp after integer-pixel rounding. Probe bounds are 158×79 px.

Evidence: `mobile-qa/ios-linear-alignment.png`, `mobile-qa/android-linear-alignment.png`. The top probe is partly overlapped by system status UI; small disconnected color regions from that overlap are not additional probes. This does not establish safe-area fidelity.

The first iOS screenshot also exposed nested container backgrounds being painted before the authored frame sizing. Paint now follows sizing but precedes absolute positioning. A second actual native build/run produced `mobile-qa/ios-linear-alignment-painted.png`, visually confirming full-width backgrounds; the five containers away from the status area measure 900×270 px (300×90 pt). The first background extends into the system safe area, a remaining behavior to verify separately. The focused IR/exporter suite passed 24/24, including modifier-order and alignment regressions. No broad capability row was promoted: stretch, wrapping/grid alignment, main-axis distribution and the full sizing matrix remain outside this measurement.

After recording these screenshots, the generated source fixtures were restored using `--growing-text`, preserving the existing accessibility fixture contract. The installed QA applications still show the alignment probe until their next rebuild/install. No simulator density or font-scale setting was changed for this run.

## Native padding and bounded background paint, 2026-09-05

Mobile flow containers now emit resolved padding using the engine's scalar/one-to-four-value top-right-bottom-left shorthand. SwiftUI applies EdgeInsets before authored frame sizing; Compose applies padding after sizing and background so the exterior dimensions and painted area stay unchanged. Invalid, non-finite, negative or unsupported arity values fail explicitly in the mobile writer. This does not claim padding semantics for absolute children or layout-none containers. The modifier order follows [Android's modifier documentation](https://developer.android.com/develop/ui/compose/modifiers).

SwiftUI container backgrounds now explicitly use `ignoresSafeAreaEdges: []`, per [Apple's background documentation](https://developer.apple.com/documentation/swiftui/view/background(ignoressafeareaedges:)). This prevents the automatic safe-area extension observed in the preceding run; it does not relocate a fixed screen or solve its overall relationship with system chrome.

The `--padding` fixture uses the same six 300×90 containers and 60×30 probes as alignment QA, with padding `[10, 20, 10, 40]`. Both applications were rebuilt, installed and launched on the existing devices. Screenshots were visually inspected and measured:

- iPhone: vertical probe x = 134, 404, 674 px against container x = 14, yielding 40, 130, 220 pt. Horizontal y = 1041, 1413, 1785 px against container y = 1011, 1323, 1635, yielding 10, 30, 50 pt. All six background bounds are now 900×270 px, including the first at y = 75; the earlier extension to y = 0 is gone.
- Android, density 420: vertical x = 105, 341, 577 px, consistent with 40, 130, 220 dp after integer constraint/alignment rounding. Horizontal y offsets = 26, 79, 131 px, consistent with 10, 30, 50 dp. All backgrounds measure 788×236 px; probes remain 158×79 px.

Evidence: `mobile-qa/ios-linear-padding.png`, `mobile-qa/android-linear-padding.png`. System status text still overlaps the first probe and creates small disconnected regions; those fragments are excluded from the six-probe comparison. The scoped IR/exporter suite passed 25/25. These native cases verify asymmetric padding with start/center/end linear alignment, not grid, wrap, RTL, every dimension mode, or all shorthand forms at runtime; no broad capability row was promoted.

Generated fixture sources were restored with `--growing-text` after measurement; installed QA apps retain the padding probe until the next install. Device settings were unchanged.

## Native mobile vector strokes, 2026-09-05

Both mobile vector emitters previously painted only fills, silently omitting strokes in the native candidate path. They now emit centered solid strokes separately from fills, using the original path commands without adding closure. SwiftUI uses an overlaid stroke and a compositing group; Compose draws the stroke after the fill and applies node opacity to the Canvas modifier instead of independently to both paints. SwiftUI's miter limit is explicitly 4 to match the owned engine's `DEFAULT_STROKE_MITER_LIMIT` and Compose's default. References: [Apple StrokeStyle](https://developer.apple.com/documentation/swiftui/strokestyle/init(linewidth:linecap:linejoin:miterlimit:dash:dashphase:)) and [Android Stroke](https://developer.android.com/reference/kotlin/androidx/compose/ui/graphics/drawscope/Stroke).

The native candidate writer now fails explicitly for non-centered or dashed vector strokes rather than silently approximating them. This is an implementation gap, not a claim that either native framework cannot render those constructs. Existing production capability gates remain unchanged. Solid width-zero strokes are omitted; invalid width/color and unsupported cap/join values fail explicitly.

The `--strokes` QA fixture contains three unfilled, open L paths, width 8, with butt/round/square caps and miter joins. Both native applications were rebuilt, installed and launched; their screenshots were visually inspected. All three strokes are visible, caps differ as authored, and no closing diagonal or filled triangle appears.

- `mobile-qa/ios-vector-strokes.png`: near-solid connected bounds are 732×252, 744×263 and 744×264 px at 3 px/pt. The one-pixel round-cap difference reflects the solid-color threshold excluding antialiased edge pixels.
- `mobile-qa/android-vector-strokes.png`: corresponding bounds are 640×220, 650×230 and 651×231 px at 420 dpi. These are thresholded interior bounds, not claimed exact vector outsets.

The focused IR/exporter suite passed 26/26 and the development build passed. Tests inspect separate stroke emission, open-path preservation, opacity placement and rejection of unimplemented alignment. Native screenshots cover opaque centered open strokes with three caps; they do not yet prove curved joins, translucent overlap compositing, out-of-bounds strokes, gradients, dashed patterns or the complete stroke-property matrix. No broad capability row was promoted. Generated fixture sources were restored with `--growing-text`; installed QA apps retain the stroke fixture until the next install. Device settings were unchanged.

## Canvas stroke-field lowering and mobile dashes, 2026-09-05

The renderer preparation boundary was treating a Canvas stroke descriptor with `width` but no `thickness` as a fill object, and was not mapping `dash` to the renderer's `dashPattern`. It now lowers both fields on the cloned render document. A real graph probe and regression test verify stroke weight 8, dashPattern `[12, 12]`, and cap/join values; another regression verifies the authored document remains unchanged. This is a render-boundary correction, not a stored-document migration.

Both mobile writers now emit centered dashed strokes for positive, even-length patterns: SwiftUI StrokeStyle dash lengths and Compose PathEffect.dashPathEffect. [Apple documents painted/unpainted dash lengths](https://developer.apple.com/documentation/swiftui/strokestyle); [Android documents alternating on/off intervals and the even-length requirement](https://developer.android.com/reference/android/graphics/DashPathEffect). Odd-length/zero-length patterns still require consistent normalization and measurement; they fail explicitly in the mobile candidate writer rather than silently changing the artwork. Non-centered strokes remain unfinished. This supersedes the preceding entry's statement that all dashed mobile strokes are unimplemented.

The `--dashes` fixture uses authored `width: 8` and `dash: [12, 12]` on the three open L paths. Native iPhone and Android applications were rebuilt, installed and launched. Both inspected screenshots contain exactly 42 disconnected dash regions, 14 per path, with distinct butt/round/square cap geometry and no closing diagonal:

- `mobile-qa/ios-vector-dashes.png`: first butt-cap dashes are 36×24 px, with x origins 134, 206, 278, giving 12 pt paint and 12 pt gap at 3 px/pt.
- `mobile-qa/android-vector-dashes.png`: first butt-cap solid interiors are 31×21 px, with x origins 106, 169, 232, giving 24 dp pattern repetition at 420 dpi. Thresholded widths exclude antialiased edge fractions.

The focused renderer/engine/IR/exporter test run passed 106/106. These screenshots measure one positive even-length pattern with three caps; they are not full dash-property or renderer-pixel parity proof, and no broad capability row was promoted. Generated sources were restored with `--growing-text`; installed apps retain the dash probe until the next install. Device settings were unchanged.

The subsequent full `bun run test` passed 348/348, with zero failures, cancellations or skips, in 235.2 seconds. The SwiftUI compilation portion passed in 164.1 seconds; Android compilation also passed. Local log: `/tmp/canvas-post-dash-full-suite.log`. The development build passed separately. This supersedes the earlier 342-test regression result for the current changes, without expanding the native visual coverage claims above.

## Odd and zero-length dash normalization, 2026-09-05

A shared `normalizeStrokeDash` now implements the numeric [SVG dash-array rules](https://www.w3.org/TR/SVG/painting.html#StrokeDasharrayProperty) at both renderer preparation and mobile emission: repeat odd-length lists, retain individual zero lengths, and remove the dash effect when all lengths are zero. Negative/non-finite/non-numeric values fail without coercion. Arrays are copied; authored documents are not rewritten. Graph regressions verify the normalized patterns reach the renderer. This supersedes the prior positive-even-only mobile restriction.

The `--dash-edge-cases` fixture exercises `[12]` with butt caps, `[0, 12]` with round caps, `[12, 0]` with butt caps, and `[0, 0]` with butt caps. Both actual applications rebuilt and ran; screenshots were inspected:

- `mobile-qa/ios-vector-dash-edge-cases.png`: row region counts 14, 27, 1, 1. The repeated odd pattern has 36×24 px dash interiors; the zero-on pattern produces dots. Zero-gap and all-zero rows have equal outer bounds but different corner coverage: 22,896 versus 23,040 near-solid pixels. The zero-gap pattern retains dash cap boundaries instead of being incorrectly collapsed into a solid stroke.
- `mobile-qa/android-vector-dash-edge-cases.png`: the same row counts 14, 27, 1, 1. Zero-gap versus all-zero near-solid areas are 17,299 versus 17,420 pixels; the same corner distinction is visible.

These are thresholded native measurements, not full renderer pixel-parity claims. The focused renderer/engine/IR/exporter/normalizer suite passed 109/109, the development build passed, and diff whitespace checks passed. Broad capability rows remain unchanged. The preceding 348-test full run predates this normalizer change. Generated fixture sources were restored with `--growing-text`; installed apps retain the edge-case probe until their next install. Device settings were unchanged.

## Aligned vector strokes and live-render integration, 2026-09-05

`measure-vector-stroke-alignment.mjs` exposed a shared renderer defect: center, inside and outside strokes on the same closed rectangular path all painted x=36..43 on a boundary at x=40. Evidence before repair: `mobile-qa/canvas-vector-stroke-alignment-before.png`. The owned engine's vector-stroke branch now clips a double-width stroke to the fill interior or its complement. The measured bands are now center x=36..43, inside x=40..47 and outside x=32..39. Reference: `mobile-qa/canvas-vector-stroke-alignment.png`. Uncached temporary stroke outlines are also released after painting.

The fork's browser visual test initially failed before stroke verification because frame-title drawing called the missing `LabelCache.getFrames`. The cache now supplies visible top-level frames and invalidates that collection alongside other labels. Its 13 unit tests pass. A new six-case solid/dashed center/inside/outside Playwright snapshot was generated, inspected, and then rerun without snapshot updates: 1/1 passed. It lives beside the existing stroke visual coverage in `vendor/open-pencil/source/tests/e2e/canvas/stroke-geometry-visual.spec.ts-snapshots/vector-stroke-alignment-openpencil-darwin.png`. No browser errors were suppressed.

Mobile emission now applies the same double-width-and-clip construction. SwiftUI uses [GraphicsContext clipping](https://developer.apple.com/documentation/swiftui/graphicscontext/clip(to:style:options:)) with an expanded stroke overlay drawing surface; Compose uses [ClipOp intersect/difference](https://developer.android.com/reference/kotlin/androidx/compose/ui/graphics/ClipOp), after painting the fill. Fill-rule is retained in the clipping path. The former blanket rejection of inside/outside mobile strokes is removed.

The `--stroke-alignment` native fixture uses 240×80 closed rectangular path boundaries and width 8, inset in a 300×120 node. Both applications rebuilt and ran, and both screenshots were inspected:

- `mobile-qa/ios-vector-stroke-alignment.png`: centered, inside and outside orange bounds are respectively 744×264, 720×240 and 768×288 px, or 248×88, 240×80 and 256×96 pt.
- `mobile-qa/android-vector-stroke-alignment.png`: corresponding thresholded interiors are 651×231, 629×209 and 671×251 px at 420 dpi; antialiased clipping boundaries account for excluded fractional edge pixels.

Focused Canvas engine/renderer/IR/exporter/normalizer tests pass 109/109. These cases do not establish curved/self-intersecting paths, holes, translucent overlap, arbitrary path coordinates beyond viewBox, or all out-of-bounds clipping behavior. No broad capability row was promoted. Generated sources were restored with `--growing-text`; installed QA apps retain the alignment fixture. The package installed in the actual Canvas controller has not yet been refreshed with these source changes.

### Full regression and installed-package follow-up

After the aligned-stroke and frame-label repairs, engine and development builds passed. The full Canvas suite passed **351/351**, with no failures, cancellations or skips, in 251.8 seconds, including both native compile fixtures (`/tmp/canvas-aligned-stroke-full-suite.log`). The public `penkra app test` passed manifest, operation help and isolated runtime checks. Public sideload of the rebuilt `dist` returned `current`: the development sideload implementation compares package SHA-256, not merely the version, so no version bump is needed.

A separate Account-owned editor QA document was created: `328a8764-d7da-42c1-951c-2ba8acf2e86a`, title `Canvas completion QA 2026-09-05`, with starter `0ac578cc-df5b-440f-9a90-27cc5637050e` and text probe `qa-rich-text`. The mutation succeeded at sequence 1562 with no reported issues. Its tab `1a4efa44-fce3-4a13-bf4e-0e764a38b97f` subsequently returned only the document shell, and `tabs.current` returned null. A prior retained tab briefly returned its full interface but then also returned a shell. This is not a live editing verification pass; no user document was edited for these probes.

Clarification: null `tabs.current` is **not** a barrier to retained-tab semantic observation or interaction. Only screenshots require visual visibility. Subsequent exact-tab snapshots and Layers/Inspect actions worked without changing that condition. The intermittent empty snapshot must be investigated separately; attributing it to visual visibility was incorrect.

The retained tab exposed `Canvas text editor` after selecting the text layer and pressing Enter. However, subsequent Enter/Escape/navigation probes did not establish saved edits. Host source inspection found that `penkra/apps/desktop/src/appTabObserver.ts` sends `Input.dispatchKeyEvent` with only `{ type, key: normalized }`. It neither supplies `code` nor parses modifier combinations; Canvas inline keyboard handlers depend on `KeyboardEvent.code`. The tool's documented key-combination examples therefore exceed this implementation. Permission to change the host repository was requested separately; no host code was changed. A Text-tool probe also created empty QA node `0:8`, visible at sequence 1564; the original text remains unchanged in saved reads.

### Transparent vector strokes at node boundaries

The `--stroke-compositing` native fixture combines curved outer contours, even-odd holes, center/inside/outside width-8 strokes, and node opacity 0.5, with geometry touching the node bounds. iOS preserved the outer strokes, but Android's `.alpha` modifier clipped strokes at node bounds. Evidence: `mobile-qa/ios-vector-stroke-compositing.png` and `mobile-qa/android-vector-stroke-compositing.png`, both inspected. This matches Android's documented [implicit offscreen clipping for layer alpha](https://developer.android.com/reference/kotlin/androidx/compose/ui/graphics/graphicsLayer.modifier).

Compose vector emission now uses one explicit canvas saveLayer around fill and stroke, with path bounds expanded for stroke joins, rather than a bounds-clipping alpha modifier. This retains group opacity instead of multiplying fill and stroke independently. The updated Android app compiled and ran; `mobile-qa/android-vector-stroke-compositing-fixed.png` was inspected and shows the complete strokes. Both native images have identical dominant interior RGB values: fill `(128,158,172)`, stroke `(245,202,165)`, background `(246,242,234)`. The exporter test suite passes 19/19, including a regression for expanded compositing bounds. Generated fixture sources were restored with `--growing-text`. Full-suite 351/351 above predates this additional Compose fix; no broad capability row was promoted.

The subsequent full suite passed 351/351 in 111.4 seconds (`/tmp/canvas-post-compositing-full-suite.log`), and the development build passed.

### Basic shapes and container opacity

The `--opacity` fixture measures an opaque rectangle, a 50%-opaque rectangle, a 300x100 ellipse, and a 50%-opaque frame containing an overlapping opaque child. Before screenshots (`mobile-qa/ios-shape-opacity.png`, `mobile-qa/android-shape-opacity.png`) exposed Android background paint outside the opacity modifier, Android's capsule approximation for non-square ellipses, and SwiftUI's non-grouped container opacity. All were visually inspected.

Android now applies container/rectangle opacity before background drawing and emits ellipses with native [drawOval](https://developer.android.com/reference/kotlin/androidx/compose/ui/graphics/drawscope/DrawScope). SwiftUI containers use [compositingGroup](https://developer.apple.com/documentation/swiftui/view/compositinggroup%28%29) before opacity. Both apps rebuilt, installed and ran. The inspected `ios-shape-opacity-fixed.png` and `android-shape-opacity-fixed.png` show true ellipses and group-composited overlapping content. Uniform interior colors are iOS blue `(128,158,172)` and orange `(245,202,165)`, Android blue `(129,158,173)` and orange `(245,202,166)`—at most one 8-bit channel step apart, consistent with the native layer-alpha quantization. Both opaque rectangles remain `(11,74,111)`.

The rectangle and ellipse **node** rows are now native for iOS and Android, supported by these non-square fixtures and prior grid/circle measurements. Their separate paint, effect, transform, layout and accessibility rows retain independent gates. Remaining unverified inventory: iOS 51, Android 50, PDF/X-4 profile 1. This is not completion of the mobile matrix. Focused capability/exporter tests pass 23/23. Generated sources were restored with `--growing-text`; installed QA apps retain the opacity fixture. The latest full-suite pass above predates these basic-shape fixes.

The subsequent full suite passed 352/352 in 114.3 seconds (`/tmp/canvas-post-shape-opacity-full-suite.log`). `verify-mobile-shape-opacity.mjs` now checks the actual screenshots: three 3:1 translucent shapes, rectangle coverage 1.0, ellipse interior coverage 0.7810 (iOS) / 0.7802 (Android), frame blue coverage 0.6000 / 0.5997 and an orange child covering the authored 200x60 of 300x100. Both fixed images pass; the pre-fix iOS image fails the orange group-compositing check, and the pre-fix Android image fails the translucent-blue count.

Text emission also omitted node opacity. Both writers now apply it to the complete rich-text expression. The extended fixture compiled and ran on both devices; inspected `mobile-qa/ios-text-opacity.png` and `mobile-qa/android-text-opacity.png` include opaque/translucent ABC samples. The verifier's `--text` mode additionally checks three glyph interiors in each sample and matching widths/heights within two antialiased pixels. Both pass with the existing one-channel-step color tolerance. iOS remains at accessibility XXL; Android remains at font scale 1.0, so these are opacity checks, not cross-platform font metric equivalence. Focused exporter tests pass 21/21; the full-suite result above predates this final text-opacity change.

The subsequent full suite passed 353/353 in 162.2 seconds (`/tmp/canvas-post-text-opacity-full-suite.log`).

### Solid-color syntax and paint alpha

### Role/module visibility and implementation authorization follow-up

The user authorized research and implementation unless a genuine product decision requires their input, and requested automatic frame-role labels and document-module labels. Reserved plan/operation/instruction/skill files remain untouched.

Frame roles are carried separately as scene metadata (`canvasRole`), rendered before the authored name and included in title hit testing. Saved names are not rewritten. The five-case role/roleless browser snapshot was generated, visually inspected and rerun without updates: 1/1 passed. The engine rebuilt with SHA-256 `80ff56b0f505985b71bacbf45626d7c3bc49ac9fc06e5df2627d546d04730def`.

Library cards load module metadata asynchronously from the public Account project state, applying subsequent CRDT updates. They do not retrieve blob inventories or projection chunks for this lookup. Requests have two-worker concurrency, revision caching, cancellation of stale callbacks and retryable errors. Focused API/module tests passed 20/20. The full suite after the label work passed 357/357 in 269.9 seconds. The development build and installed-package test passed, and sideload returned `current`. Retained-tab snapshots returned only the Canvas document shell. A subsequent public document-open call did not return during the observation window; its orchestration wait was stopped without retrying the navigation. Installed UI verification is not claimed.

DEC9 now accepts `export: default|image`, rejects the withdrawn node `live` value, applies whole-frame image overrides and reports raster consequences. Rasterized subtrees do not need native descendant evaluation. Default does not override raster verdicts. DEC6 restricts canonical axes to appearance/viewport and removes the built-in interaction-hover variable. The Chrome fixture verifies responsive appearance/viewport behavior and unchanged gap on hover. Combined schema/IR/exporter/Chrome checks passed 44/44 after correcting a stale `theme`-axis test fixture.

The separately authorized Penkra host keyboard repair sends key/code/modifier/virtual-key fields rather than a literal shortcut string, following [Chromium Input.dispatchKeyEvent](https://chromedevtools.github.io/devtools-protocol/tot/Input/#method-dispatchKeyEvent). Host keyboard and observer tests pass 25/25, including exact retained-tab targeting. A fresh Penkra Dev 3 launch was requested for host QA; no runtime keyboard pass is claimed yet. No host version, release or shared instruction was changed.

The subsequent full suite passed 354/354 in 119.9 seconds (`/tmp/canvas-post-solid-colors-full-suite.log`), and the development build passed. This result predates the DrawingML unit-scale fix described here.

### Additional PowerPoint vector verification

After the winding fix the full suite passed 354/354 in 251.2 seconds (`/tmp/canvas-post-winding-full-suite.log`). The later polygon adapter repair is covered separately below.

The CanvasKit reference then exposed a polygon mismatch: the authored five-sided geometry rendered as the legacy default triangle. The scene adapter now maps polygons with authored geometry to VECTOR and parses/scales their geometry and winding rule exactly like paths. Legacy geometry-less polygon handling is unchanged. The focused adapter unit test passes, a new browser snapshot was generated and visually inspected, and the no-update rerun passes 1/1. An initial rerun raced the package rebuild and failed on a temporarily absent package dist entry; the successful rerun ran after the build completed. Post-rebuild Canvas engine/vector tests pass 59/59. The rebuilt engine SHA-256 is `eec067b09fefaa85759dbe60c2cfa270f19d8d42276d7f27cb7ff9a41b8c3c3c`. The regenerated and inspected reference is `powerpoint-qa-fixed/canvas-native-vectors.png`; the pre-fix triangle remains in the original QA directory as negative evidence. The new PPTX still contains five native custom geometries and zero raster requests.

`scripts/verify-powerpoint-vectors.mjs` generates five native custom-geometry shapes and a CanvasKit reference in `research/powerpoint-qa`. A curved donut exposed float32 containment-probe instability at the writer's 100000-unit coordinate scale. Winding orientation now runs at unit scale before scaling to DrawingML coordinates. The coverage regression now exercises the actual writer scale, including the failing curved donut; focused vector/exporter tests pass 26/26. The diagnostic PPTX has five `a:custGeom` elements, no picture elements and no raster requests. Microsoft PowerPoint opened it successfully and its object model reports one slide with five shapes.

Native PowerPoint visual verification remains incomplete: presentation `save as PNG` returned without producing an output; shape `save as picture` returned parameter error -50 for both POSIX and HFS paths. System Events UI inspection returned assistive-access denial -1719, and `screencapture` could not create an image from the display. None of these is counted as a visual pass. The original fixture remains open; no unrelated presentation was changed.

Mobile conversion previously ignored a solid paint object's opacity and mishandled four-digit RGBA hex strings. It now accepts 3/4/6/8-digit hex, numeric rgb/rgba, transparent, and single active solid-paint arrays; structured paint opacity multiplies color alpha. Disabled paints are omitted. Invalid colors/opacity and unimplemented multiple active paints fail explicitly rather than generating invalid code or silently disappearing. This is not a claim that multi-paint rendering is a native format limitation. Reference: [CSS Color 4 hex notation](https://www.w3.org/TR/css-color-4/#hex-notation).

The `--solid-colors` fixture renders equivalent `#3698`, `#33669988`, numeric rgba and an opaque color with paint opacity 8/15, followed by color alpha multiplied by paint opacity 0.5 and a transparent rectangle. Both apps compiled and ran; `mobile-qa/ios-solid-colors.png` and `mobile-qa/android-solid-colors.png` were inspected. The first four rectangle interiors match exactly at `(142,167,191)` on both devices. The multiplied-alpha fifth rectangle is `(194,205,212)` on iOS and `(194,204,213)` on Android, within one channel step. The transparent sixth rectangle paints no visible mark. Focused exporter tests pass 22/22, covering shorthand alpha, numeric rgba, single-paint arrays, paint alpha and invalid inputs. Broad fill rows remain gated; this does not establish every color space, multiple paints or gradients.
